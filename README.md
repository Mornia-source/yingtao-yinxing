# 樱桃音形

自制 Rime 中州韵方案：自然码双拼 + 98五笔首末字根形码。

[MIT License](LICENSE)（第三方词库数据保留各自 GPLv3 协议，见 LICENSE 文件说明）。

## 编码规则

- **单字**：自然码双拼全码（2位，不含辅助码） + 98五笔该字首位字根 + 末位字根（2位，不含末笔识别码）。例：廷=tytp，啊=aakk，我=woty，全=qrwg。双拼2位一打完，同音字就已经按字频排好队出现在候选里了（比如打"ui"就能看到 是/时/事/使/式……），不用非等形码也打完才能选字；形码留给需要精确定位某个字的时候用。
- **二字词**：简码 = 前字前两位 + 后字首位（3位）；全码 = 前字前两位 + 后字前两位（4位）。词组一律从3位编码起，2位编码专属于单字双拼（见上一条），这样双拼2位刚打完时，候选里只会出现同音单字，不会被词组抢占。
- **6位精确码（所有词长通用）**：任何词打完 4 位全码后如果还有重码，可以**继续打 2 位形码 —— 最后两个字的首字根**，精确定位。例：简单=jmdj**tu**，时间=uijm**ju**，不知道=bvdk**tu**，非常好=fihk**iv**，妙不可言=mbky**sy**，中华人民共和国=vhrg**tl**。
- **候选数上限 6**：一页最多显示 6 个候选（`menu/page_size: 6`）。
- **候选排序：字数少的优先，全码精确匹配的候选例外**。同一次输入下，还需要继续补全的候选（比如4码全码打完后冒出来的6码提示）按字数少优先排；但只要某个候选的编码正好是当前打的这几个字符（不用再多打），它就跳出字数排序、只按权重和其它精确匹配的候选比较——用 `cand.comment` 是否为空判断是不是精确匹配。这样"晪"这种冷僻单字不会仅仅因为字数少，就永远压在"添加"这类高频词前面。
- **临时反查**：打 `z` 再接全拼可以用拼音打字（〔全拼〕）；打 `/` 再接五笔码可以用纯 98 五笔打字（〔五笔98〕）。`z` 是正常码位（"在/最/走"等417个编码都以z开头），实测 `table_translator` 和 `reverse_lookup_translator` 会同时给出候选、互不吞码，配合权重分档该赢的还是赢；`/` 后面不接字母时仍然出标点。
- **单键硬性首选**：只按一个字母时，第一候选固定为下表的字，不受词频和调频影响：`q去 w我 e二 r人 t他 y一 u是 i出 o哦 p平 a啊 s三 d的 f发 g个 h和 j就 k可 l了 z在 x小 c才 v这 b不 n你 m没`。其余编码照常按词频排序、并保留 user_dict 智能调频。
- **三字词**：全码 = 前两字首码 + 第三字前两位；简码 = 三字首码。例：不知道=bvdk/bvd，非常好=fihk/fih。
- **四字词**：四字首码。例：妙不可言=mbky。
- **五字及以上**：前三字首码 + 末字首码。例：踏破铁鞋无觅处=tpti。
- **ü 韵母 u/v 都能打**：j/q/x/y 后面的"u"实际是"ü"（续xù、语yǔ、去qù、句jù……），标准双拼用 v 表示，但因为 x/j/q/y 从不会真的和"u"这个韵母搭配，所以打 u 或 v 都能选中同一个字/词——继续=jixv 或 jixu 都行，粤语=ytyv 或 ytyu 都行。有简码的词全码依然保留、不会被简码顶掉。
- **零声母 an/en/ai/ei/ao/ou 直接按字面打**：这几个韵母本身拼音拼写就正好是2个字母，不再套"重复声母、压缩韵母"那一套，直接按字母打——按=an，安=an，爱=ai，诶=ei，奥=ao，欧=ou。（a/e/o 单独或带 ng 的 a/ang/e/eng 仍然需要重复首字母来腾出韵母键位，比如 啊=aa、昂=ah。）

## 安装方法

1. 安装 [小狼毫 Weasel](https://github.com/rime/weasel/releases)（Windows，且需要支持 `lua_filter` 的构建，比如98五笔版小狼毫自带的那种）或其他支持 lua 插件的中州韵前端。
2. 把本目录下的文件复制到 Rime 用户目录（Windows 一般是 `%APPDATA%\Rime`）：
   - `yingtao.schema.yaml`
   - `yingtao.dict.yaml`
   - `yingtao_chars.dict.yaml`
   - `yingtao_words.dict.yaml`
   - `lua/char_priority.lua`（复制到用户目录下的 `lua` 子文件夹，没有就新建）
   - `wubi98.schema.yaml` + `wubi98.dict.yaml`（供 `/` 五笔反查用；它不必加进 `schema_list`，樱桃音形的 `dependencies` 会带着它一起编译）
3. 编辑用户目录下的 `default.custom.yaml`（没有就新建），加入：

   ```yaml
   patch:
     schema_list:
       - schema: yingtao
   ```

4. 右键托盘图标"重新部署"。**注意**：小狼毫的"重新部署"只会重新编译码表文件，正在运行的 WeaselServer 进程不一定会重新加载编译结果；如果部署后候选看起来还是旧的，需要手动重启一次 WeaselServer 进程（或注销/重启电脑）。

## 数据来源与生成方法

**单字**

- **98五笔字根参考（去末笔识别码）**：用户自行整理的"通用规范汉字8105字"参考表，每个字对应的98五笔码已手动去掉末笔识别码、只保留真实字根序列，是单字形码的首选数据源（`sources/wubi98_noident_8105.txt`）。
- 单字的拼音与字频：[iDvel/rime-ice](https://github.com/iDvel/rime-ice) 的 8105 常用字表。
- 官方98五笔全码：[lotem/rime-wubi98](https://github.com/lotem/rime-wubi98)、汉字结构拆分：[hanzi-chai/pychai](https://github.com/hanzi-chai/pychai) —— 仅作为上面参考表覆盖不到的极少数生僻字（约32个）的备用近似算法。

**词组**（v3.0 起统一用雾凇词库）

- **词表 / 注音 / 词频**：全部来自 [iDvel/rime-ice 雾凇拼音](https://github.com/iDvel/rime-ice) 的基础词库 `base` + `others`（快照版本 2026-07-11），约 54 万条多字词。格式是「词 + 逐字拼音 + 词频」，注音是雾凇手工校订过的，多音字比通用注音库更可靠（银行 yin hang、重庆 chong qing、会计 kuai ji、音乐 yin yue 都是对的），所以直接用它；个别注音缺失才退回 pypinyin 兜底。樱桃音形的编码全部由本文档「编码规则」一节的规则现算。
  > **为什么放弃了 openfly（小鹤词库）**：v2.0 曾用过 [amorphobia/openfly](https://github.com/amorphobia/openfly) 的词表。后来查明——小鹤官方词库闭源且禁止反编译，openfly 是对 v9 时代的复刻、已停更（最后更新 2024-09）；社区里还在维护的所谓「小鹤词库」方案（rime-fast-xhup、rime-crane 等）用的其实**都是雾凇的词库**，只是套了小鹤的编码规则。所以「小鹤重码少」是**编码方案 + 词表精简**的功劳，词库本身并无独到之处。既然雾凇长期维护（2026-07 仍在更新）、注音更准、覆盖更全，就直接用它。
- **词频下限**：`build/gen_words.py` 里的 `MIN_FREQ`，默认 0（全收）。想压重码可以往上调，效果见下方「重码与候选数」。

**生成脚本**

`../build/` 下的 Python 脚本可重新运行以更新码表：`shape.py`（形码计算）、`zrm.py`（自然码双拼算法）、`tier_boost.py`（码长分档权重）、`gen_chars.py` / `gen_words.py`（生成码表）。`../sources/` 存放脚本依赖的原始数据快照。

生成词库需要 pypinyin：

```bash
pip install pypinyin
```

## 已知局限

单字形码现在以用户提供的"98五笔去识别码"参考表为准（覆盖 8105 字表中的 8494 字，直接取参考表里字根序列的首字母+末字母，结果是精确的，不再依赖猜测）。只有约32个不在这份参考表里的生僻/异体字（如 車、卻、咲、牠等，本来就不属于通用规范汉字表），才会退回到"拆字库交叉验证 + 官方全码首尾近似"的旧算法，可能存在少量偏差；还有3个极生僻扩展汉字查不到五笔码，未收录进单字表。

如果后续发现某个字的形码不对，最快的办法是直接在 `yingtao_chars.dict.yaml` 里手动改那一行的码；如果是参考表本身就没有的生僻字，也可以在 `build/shape.py` 的 `CHAR_SHAPE_OVERRIDE` 里加一条手动修正。

## 编码位数越少优先级越高

这条规则由两套机制配合完成：

1. **`build/tier_boost.py`（词典权重加成）**：候选自身编码位数分三档加成——2位（单字双拼）+1亿，3位（词组简码）+5千万，4位（全码）不加成，同档内再按词频排序。这样"2位单字 > 3位简码 > 4位全码"这个优先级本身就写进了词典权重里。另外还有 1 位档（+10亿）用于「单键硬性首选」那 26 个字。这个文件里留了一份 `PRIORITY_OVERRIDES` 空名单，格式是 `("编码", "文本"): 加成权重`，将来遇到某个编码想强制指定首选、又不想依赖排序机制时可以往里加。
2. **`lua/char_priority.lua`（候选过滤器）**：Rime 的补全(completion)候选是按编码在字根树里的先后顺序枚举出来的，并不会跨编码统一按词典权重重排——这意味着只靠第1条加权重，"的"也不一定能排到"掉落"前面。这个过滤器在候选队列最前面一小段（默认20个）里做两级排序：先把「全码精确匹配」的候选整体排到「还需要补全」的候选前面（按 `cand.comment` 是否为空判断），精确匹配内部纯按 `quality` 排、不再管字数；补全候选则按「字数少优先，同字数内按 `quality`」排。**这里踩过坑**：直接对候选调用 `table.sort` 出过一次问题——候选流里偶尔混入 `quality` 不是普通数字的候选，导致排序中途出错但已经做了部分交换，候选顺序反而更乱。现在的写法是先把所有候选的 `quality` 取到一个数组里逐个校验，校验不通过就直接放弃排序、原样透传，确保任何情况下都不会比"完全不排序"更差。

## 6 码精确码的由来

双字词打 4 码全码后如果还有重码，可以**继续打 2 位形码**（前字首字根 + 后字首字根）精确定位，不需要任何引导符。当初选取码方式时的实测对比（雾凇双字词）：

| 取码方式 | 唯一码率 | 最大重码 |
|---|---|---|
| 4 码全码（不加形码） | 25.7% | 34 |
| **6 码：字1首根 + 字2首根** | **93.5%** | **6** |
| 6 码：字1首根 + 字2末根 | 91.7% | 5 |
| 6 码：字1末根 + 字2末根 | 89.0% | 6 |
| 5 码：只加 1 位 | 66% | 14~16 |

选「两个字都取首根」：区分度最高，规则也最统一好记（一条规则而不是两条），并且和五笔词组规则（`AaAbBaBb`，都从头取）一致。分析脚本见 `build/analyze_2char.py`。

**为什么不需要 `;` 引导符**：librime 的 `auto_select` 有两条独立路径——`AutoSelectUniqueCandidate` 要求**候选恰好只有 1 个**才自动上屏（`seg.menu->Prepare(2) == 1`），而 `AutoSelectAtMaxCodeLength` 按 `max_code_length` 触发且不管候选数量。所以把 `max_code_length` 抬到 6、`auto_select_pattern` 写成 `^[a-z]{4}$|^[a-z]{6}$` 之后：4 码唯一 → 直接上屏（本来也不需要 6 码）；4 码撞车 → 不上屏，可以继续打第 5、6 码；6 码唯一 → 上屏。天然自洽。

## 重码与候选数

候选数上限设为 6（`menu/page_size: 6`）。换用雾凇全量词库后，各词长的重码实测如下（雾凇 base 54 万词）：

| 词长 | 词数 | 全码唯一率 | 最大重码 | 排在第 7 位之后（需翻页）的词 |
|---|---|---|---|---|
| 二字词（4 码） | 14.4万 | 23.3% | 32 | 6,093 |
| **二字词（6 码精确码）** | 14.3万 | **93.5%** | **6** | **0** |
| 三字词（4 码） | 16.2万 | 23.0% | 43 | 16,942 |
| **三字词（6 码精确码）** | 16.2万 | **77.0%** | **33** | **1,734** |
| 四字词（4 码） | 16.9万 | 37.7% | 34 | 3,859 |
| **四字词（6 码精确码）** | 16.9万 | **86.8%** | **17** | **123** |
| 五字以上（4 码） | 7.8万 | 52.9% | 193 | 2,099 |
| **五字以上（6 码精确码）** | 7.8万 | **89.6%** | **39** | **720** |

6 码精确码统一取「全码 + 最后两个字的首字根」，各词长打完 4 码撞车后都可以续打这 2 位精确定位。二字词效果最好（93.5% 唯一、零翻页）；三字词提升也很明显（23.0%→77.0%），但因为候选基数更大、重码词更多，仍有少量残余重码，需要的话还能再往下翻页。

如果仍嫌 4 码阶段候选太杂，还可以调高 `build/gen_words.py` 里的 `MIN_FREQ` 砍掉低频词（雾凇 base 里不少低频条目是生僻人名/地名/品牌）。实测词频下限对「4 码阶段需要翻页的词数」的影响：

| 词频下限 | 三字词需翻页 | 四字词需翻页 | 五字以上需翻页 |
|---|---|---|---|
| 0（当前） | 16,202 | 3,913 | 2,099 |
| ≥50 | 9,068 | 1,611 | 595 |
| ≥100 | 7,570 | 1,253 | 371 |
| ≥300 | 4,846 | 600 | 184 |
| ≥1000 | 2,581 | 245 | 83 |

## 平均按键数：和全拼、纯双拼比到底有没有优势

词表扩到雾凇全量之后，重码率上升、部分词要续打到 6 位，这是不是意味着效率不如全拼/双拼了？实测数据（按词频加权，取每个词能用的最短编码，脚本见 `build/keystroke_compare.py`）：

| 方案 | 平均按键数/词 |
|---|---|
| **樱桃音形** | **3.17** |
| 纯双拼（固定2键/字） | 5.15 |
| 全拼（约3键/字，保守估计） | 7.73 |

分词长看更明显——2/3 字词大多能走 3 位简码，4 字词走 4 位全码，都低于双拼"字数×2"的定长开销：

| 词长 | 樱桃音形平均按键 | 纯双拼平均按键 |
|---|---|---|
| 2字 | 3.00 | 4.00 |
| 3字 | 3.00 | 6.00 |
| 4字 | 4.00 | 8.00 |
| 5字 | 4.00 | 10.00 |
| 6字 | 4.00 | 12.00 |

**6 位只是碰撞时的兜底**，多数词仍然走 3-4 位的简码/全码；真正需要打满 6 位的只是重码率上升后新增的那一小撮词，不是全部词都变长了。这份统计是"最短可用编码"的理想值，没有算上挑候选、翻页等额外开销，但全拼/双拼同样有这个问题（重码一样要选词），所以作为方案之间的相对比较是公平的。

## 智能分词 / 长句连续输入 / 四码顶字

- `translator/enable_sentence: true`：允许词与词之间不用分隔符直接连打，输入法会自动按码表做分词组合成整句。
- `speller/auto_select` + `auto_select_pattern`：编码唯一时自动上屏（4 码或 6 码），有重码时不会被提前打断，可以继续挑候选或续打。
- `engine/filters` 里 `uniquifier` 负责去重——双字词同时有 4 码和 6 码词条，打 4 码时它自己的 6 码会作为补全候选再冒出来一次。
- 三个开关（中西文、半全角、繁简）都加了 `reset: 0`，避免继承别的方案里的全角/繁体状态，导致回车提交出来的字母变成全角。

## 中文标点

标点映射直接写死在 `yingtao.schema.yaml` 的 `punctuator` 段里，不再 `import_preset`——因为同一个 Rime 用户目录下别的方案（wubi98_ci 等）共用 `punctuation.yaml`，写死可以避免互相干扰。

中文模式（`half_shape`）下符号键的输出：

| 键 | 输出 | 键 | 输出 | 键 | 输出 |
|---|---|---|---|---|---|
| `!` | ！ | `$` | ￥ | `^` | …… |
| `(` `)` | （） | `_` | —— | `[` `]` | 「」（可翻页选【】） |
| `\` | 、 | `:` | ： | `;` | ； |
| `'` | ‘’ | `"` | “” | `<` `>` | 《》 |
| `,` | ， | `.` | 。 | `?` | ？ |
| `/` | 、（后面接字母则转为五笔反查） | `{` `}` | 『』 | `` ` `` | · |

`@ # % & * - + = |` 保持原样输出半角，符合中文写作里这些符号本来就用半角的习惯。切到西文模式（`ascii_style`）时全部输出半角原字符。

## 待办

- **6位精确码的调频没有反哺回4位全码**：Rime 的自适应调频（user_dict）是按"实际打的那串编码"记学习值的，打6码选中的词，学到的频率只会提升它自己在6码下的排位，不会传导到4码。要做到"这次用6码选了'添加'，下次打4码'tmjw'时'添加'也排到'晪'前面"，需要额外写一个 lua 处理器：拦截提交事件，把6码精确码的选择计数按去掉最后2位形码的规则映射记到4码上，再让排序读取这份计数。同样是新增有一定风险的 lua 组件，还没做。

  原始需求描述（用户习惯反哺）：用户习惯，6位扩充码的使用习惯应该记录到原本的四位全码当中。例如，输入"tmjw"，优先显示"晪"单字，那么用户想要输入"添加"就需要输入"tmjwie"，那么此时由于"tmjwie"是唯一码，会直接上屏录入，不过在下一次用户输入"tmjw"后，"添加"就应该在"晪"的前面，因为此时全码"tmjw"的候选词当中，"添加"被用户调用次数最多，即使上一次用户输入"添加"采用的是6位扩充码。
- 个别词如果没收录，可以自己在 `yingtao_words.dict.yaml` 里按同样规则追加。
